---
import Layout from '../../layouts/Layout.astro';

const title = '在线图片压缩 — 减小 JPG、PNG、WebP 文件体积';
const description = '在线压缩图片，在保持画质的前提下减小文件体积。支持 JPG、PNG、WebP。所有处理在浏览器本地完成。';
---

<Layout title={title} description={description} seoType="WebApplication" lang="zh">
  <section class="hero">
    <h1>在线图片压缩</h1>
    <p>在保持画质的前提下减小文件体积。支持 JPG、PNG、WebP。</p>
  </section>

  <div class="tabs-wrap">
    <div class="tabs" role="tablist">
      <a href="/zh/" class="tab-btn" role="tab">格式转换</a>
      <a href="/zh/compress" class="tab-btn active" role="tab" aria-selected="true">压缩图片</a>
      <a href="/zh/crop" class="tab-btn" role="tab">图片裁切</a>
      <a href="/zh/watermark" class="tab-btn" role="tab">添加水印</a>
      <a href="/zh/resize" class="tab-btn" role="tab">尺寸调整</a>
      
      <a href="/zh/social" class="tab-btn" role="tab">社媒</a>
      <a href="/zh/web-optimizer" class="tab-btn" role="tab">开发</a>
      <a href="/zh/ecommerce" class="tab-btn" role="tab">电商</a>
      <a href="/zh/articles" class="tab-btn" role="tab">技巧文章</a>
    </div>
  </div>

  <div class="ad-wrapper">
    <!-- 横向广告位 -->
    <ins class="adsbygoogle"
         style="display:block"
         data-ad-client="ca-pub-4519467063111183"
         data-ad-slot="2533515795"
         data-ad-format="auto"
         data-full-width-responsive="true"></ins>
  </div>
<div class="content">
    <article class="tool-card" aria-labelledby="compress-title">
      <h2 id="compress-title" class="sr-only">图片压缩工具</h2>

      <div class="upload-area" id="uploadArea" role="button" tabindex="0" aria-label="上传图片">
        <svg class="upload-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
          <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
          <polyline points="17 8 12 3 7 8"/>
          <line x1="12" y1="3" x2="12" y2="15"/>
        </svg>
        <p>点击上传图片，或拖拽图片到此处</p>
        <p class="hint">支持 JPG、PNG、WebP、GIF、BMP 等格式</p>
        <input type="file" id="fileInput" accept="image/*" />
      </div>

      <div id="fileInfo" class="file-info" style="display:none"></div>

      <div class="control-group">
        <label for="compressQuality">压缩质量</label>
        <div class="range-row">
          <input type="range" id="compressQuality" min="1" max="100" value="70" aria-label="压缩质量">
          <span class="range-value" id="compressQualityVal">70%</span>
        </div>
        <div class="desc">数值越低，文件越小，但画质损失越大</div>
      </div>

      <div class="preview-area" id="compressPreviewArea" style="display:none">
        <figure class="preview-box">
          <figcaption class="label">原图</figcaption>
          <div id="compressOrigPreview"><span class="placeholder">请上传图片</span></div>
          <div id="compressOrigSize" class="size-label"></div>
        </figure>
        <figure class="preview-box">
          <figcaption class="label">压缩结果</figcaption>
          <div id="compressResultPreview"><span class="placeholder">点击压缩按钮</span></div>
          <div id="compressResultSize" class="size-label"></div>
        </figure>
      </div>

      <div class="btn-group">
        <button class="btn btn-primary" id="compressBtn" disabled>压缩</button>
        <button class="btn btn-secondary" id="compressDownloadBtn" disabled>下载</button>
      </div>
    </article>
  </div>
</Layout>

<script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-4519467063111183"
     crossorigin="anonymous"></script>
<script is:inline>
     (adsbygoogle = window.adsbygoogle || []).push({});
</script>

<style>
  .ad-wrapper {
    background: var(--bg-secondary);
    border-radius: var(--radius);
    min-height: 90px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 16px auto;
    border: 1px dashed var(--border);
    max-width: var(--max-w);
    padding: 0 24px;
  }
  .hero { padding: 72px 24px 56px; text-align: center; max-width: var(--max-w); margin: 0 auto; }
  .hero h1 { font-size: clamp(32px, 6vw, 48px); font-weight: 700; letter-spacing: -0.02em; line-height: 1.1; margin-bottom: 12px; }
  .hero p { font-size: clamp(15px, 2.5vw, 19px); color: var(--fg-secondary); line-height: 1.5; }
  .tabs-wrap { max-width: var(--max-w); margin: 0 auto; padding: 0 24px 24px; }
  .tabs { display: flex; gap: 2px; background: var(--bg-secondary); border-radius: var(--radius); padding: 4px; overflow: hidden; }
  .tab-btn { padding: 7px 12px; border-radius: 6px; background: transparent; color: var(--fg-secondary); font-size: 13px; font-weight: 500; text-decoration: none; transition: var(--transition); white-space: nowrap; }
  .tab-btn:hover { color: var(--fg); }
  .tab-btn.active { background: var(--bg); color: var(--fg); box-shadow: var(--shadow-sm); }
  .content { flex:1; max-width: var(--max-w); margin: 0 auto; padding: 0 24px 64px; width: 100%; }
  .tool-card { background: var(--bg); border:1px solid var(--border-light); border-radius: var(--radius); padding: 28px; }
  .sr-only { position: absolute; width:1px; height:1px; padding: 0; margin: -1px; overflow: hidden; clip: rect(0,0,0,0); white-space: nowrap; border-width: 0; }
  .upload-area { border: 1.5px dashed var(--border); border-radius: var(--radius); padding: 52px 24px; text-align: center; cursor: pointer; transition: var(--transition); background: var(--bg); }
  .upload-area:hover, .upload-area:focus { border-color: var(--accent); background: rgba(0,113,227,0.02); outline: none; }
  .upload-icon { width: 44px; height: 44px; margin: 0 auto 12px; color: var(--fg-tertiary); }
  .upload-area p { font-size: 15px; font-weight: 500; margin-bottom: 4px; }
  .hint { font-size: 13px; color: var(--fg-secondary); font-weight: 400 !important; }
  .file-info { font-size: 12px; color: var(--fg-secondary); padding: 8px 0; text-align: center; }
  input[type="file"] { display: none; }
  .control-group { margin-bottom: 20px; }
  .control-group label { display: block; font-size: 13px; font-weight: 600; margin-bottom: 6px; }
  .control-group .desc { font-size: 12px; color: var(--fg-secondary); margin-top: 4px; }
  input[type="range"] { width: 100%; accent-color: var(--accent); }
  .range-row { display: flex; align-items: center; gap: 12px; }
  .range-value { font-size: 14px; color: var(--accent); font-weight: 600; min-width: 40px; text-align: right; }
  .preview-area { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; margin: 20px 0; }
  .preview-box { background: var(--bg-secondary); border-radius: var(--radius); padding: 16px; text-align: center; min-height: 180px; display: flex; flex-direction: column; align-items: center; justify-content: center; }
  .preview-box .label { font-size: 11px; font-weight: 600; color: var(--fg-tertiary); text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 10px; width: 100%; text-align: left; }
  .preview-box img { max-width: 100%; max-height: 240px; object-fit: contain; border-radius: 4px; }
  .placeholder { font-size: 13px; color: var(--fg-tertiary); }
  .size-label { font-size: 12px; color: var(--fg-secondary); margin-top: 8px; }
  .size-label .saved { color: #34c759; font-weight: 600; }
  .size-label .bigger { color: #ff3b30; font-weight: 600; }
  .btn { display: inline-flex; align-items: center; justify-content: center; gap: 6px; padding: 11px 22px; border-radius: var(--radius-sm); font-size: 14px; font-weight: 500; border: none; cursor: pointer; transition: var(--transition); font-family: var(--font); }
  .btn-primary { background: var(--accent); color: #fff; }
  .btn-primary:hover { background: var(--accent-hover); }
  .btn-primary:disabled { opacity: 0.4; cursor: not-allowed; }
  .btn-secondary { background: var(--bg-secondary); color: var(--fg); border:1px solid var(--border); }
  .btn-group { display: flex; gap: 8px; flex-wrap: wrap; margin-top: 16px; }
  @media (max-width: 600px) {
    .hero { padding: 48px 20px 40px; }
    .tabs-wrap { padding: 0 16px 16px; }
    .content { padding: 0 16px 48px; }
    .tool-card { padding: 20px; }
    .preview-area { grid-template-columns: 1fr; }
  }
</style>

<script is:inline>
  const $ = id => document.getElementById(id);
  let sourceFile = null, sourceImage = null, resultBlob = null;

  function formatSize(bytes) {
    if (bytes < 1024) return bytes + 'B';
    if (bytes < 1024*1024) return (bytes/1024).toFixed(1) + 'KB';
    return (bytes/1024/1024).toFixed(2) + 'MB';
  }

  function imageToCanvas(img) {
    const c = document.createElement('canvas');
    c.width = img.naturalWidth; c.height = img.naturalHeight;
    c.getContext('2d').drawImage(img, 0, 0);
    return c;
  }

  $('compressQuality').addEventListener('input', function() {
    $('compressQualityVal').textContent = this.value + '%';
  });

  $('uploadArea').addEventListener('click', () => $('fileInput').click());
  $('uploadArea').addEventListener('dragover', e => { e.preventDefault(); $('uploadArea').classList.add('dragover'); });
  $('uploadArea').addEventListener('dragleave', () => $('uploadArea').classList.remove('dragover'));
  $('uploadArea').addEventListener('drop', e => {
    e.preventDefault(); $('uploadArea').classList.remove('dragover');
    if (e.dataTransfer.files.length) handleFile(e.dataTransfer.files[0]);
  });
  $('fileInput').addEventListener('change', () => {
    if ($('fileInput').files.length) handleFile($('fileInput').files[0]);
  });

  function handleFile(file) {
    if (!file.type.startsWith('image/')) return;
    sourceFile = file;
    const reader = new FileReader();
    reader.onload = e => {
      const img = new Image();
      img.onload = () => {
        sourceImage = img;
        $('fileInfo').textContent = file.name + ' · ' + img.naturalWidth + '×' + img.naturalHeight + ' · ' + formatSize(file.size);
        $('fileInfo').style.display = 'block';
        $('compressPreviewArea').style.display = 'grid';
        $('compressOrigPreview').innerHTML = '<img src="' + img.src + '" alt="原图预览" loading="lazy">';
        $('compressOrigSize').textContent = '大小：' + formatSize(file.size);
        $('compressResultPreview').innerHTML = '<span class="placeholder">点击压缩按钮</span>';
        $('compressResultSize').textContent = '';
        $('compressBtn').disabled = false;
        $('compressDownloadBtn').disabled = true;
      };
      img.src = e.target.result;
    };
    reader.readAsDataURL(file);
  }

  $('compressBtn').addEventListener('click', async () => {
    if (!sourceImage) return;
    const quality = parseInt($('compressQuality').value) / 100;
    const canvas = imageToCanvas(sourceImage);
    canvas.toBlob(blob => {
      if (!blob) return;
      resultBlob = blob;
      const url = URL.createObjectURL(blob);
      $('compressResultPreview').innerHTML = '<img src="' + url + '" alt="压缩结果" loading="lazy">';
      const newSize = blob.size;
      const origSize = sourceFile.size;
      const ratio = ((1 - newSize / origSize) * 100).toFixed(1);
      let sizeHtml = '大小：' + formatSize(newSize);
      if (ratio > 0) {
        sizeHtml += ' <span class="saved">节省 ' + ratio + '%</span>';
      } else {
        sizeHtml += ' <span class="bigger">+' + Math.abs(ratio) + '%</span>';
      }
      $('compressResultSize').innerHTML = sizeHtml;
      $('compressDownloadBtn').disabled = false;
      $('compressDownloadBtn').dataset.url = url;
    }, 'image/jpeg', quality);
  });

  $('compressDownloadBtn').addEventListener('click', () => {
    const btn = $('compressDownloadBtn');
    if (!btn.dataset.url) return;
    const a = document.createElement('a');
    a.href = btn.dataset.url;
    const name = sourceFile ? sourceFile.name.replace(/\.[^.]+$/, '') : 'image';
    a.download = name + '_compressed.jpg';
    a.click();
  });
</script>
