# 2026-05-27

## 图片百宝箱 - 广告位精简与优化

- **需求**：根据代码审查建议，精简广告位、修复isFluid bug、优化移动端体验
- **改动**：
  1. 移除左右侧边栏广告位（ad-left, ad-right）及底部广告位（ad-bottom）
  2. 仅保留中部横向广告位（ad-after-tabs, ad-slot: 2533515795）
  3. 修复 `isFluid` 未定义 bug（JS中引用了未解构的变量）
  4. 简化 `initAds()` 函数，移除动态创建 ins 的复杂逻辑
  5. 删除 `.ad-side`、`.ad-side-inner`、`.ad-placeholder`、`.ad-bottom` CSS
  6. 简化 `page-layout` 布局，去掉双侧边栏 flex 结构
  7. 添加移动端广告位优化：`@media (max-width: 600px)` 缩小 padding 和 min-height
  8. 清理 i18n 中废弃的广告位翻译文本（adPlaceholderA/B/Bottom/Main）
- **已推送到GitHub**：commit 8d8f72c

- **注意**：macOS沙盒对 `.git` 目录的 `com.apple.provenance` 扩展属性会阻止 git write，需 clone 到 /tmp 后推送

## 图片百宝箱 - 文章双语支持

- **需求**：网站面向海外，文章需要英文翻译且SEO友好
- **改动**：
  1. DEFAULT_ARTICLES 每篇文章增加 `titleEn`/`summaryEn`/`tagsEn` 字段
  2. 英文标题和摘要使用高搜索量SEO关键词（WebP, PNG, JPG, image compression, watermark, resize image 等）
  3. 英文标签使用海外用户常搜词（如 "Image Format Guide" 代替 "格式科普"）
  4. `renderArticles()` 根据当前语言选择显示内容，无英文时回退中文
  5. 文章编辑弹窗新增英文标题/摘要/标签字段
  6. i18n 新增 `modalTitleEn`/`modalSummaryEn`/`modalTagsEn` 键
  7. ARTICLE_VERSION 升至 6，loadArticles 迁移时合并英文字段到已有文章
  8. save/edit 逻辑支持英文字段（可选，有则保存）
- **已推送到GitHub**：commit 422746b
